(ocamllex
  (modules lexer))

(menhir
 (modules parser)
 (flags --table --strict))

(library
 (name stlc2m)
 (inline_tests)
 (modules server ast diag checker parser lexer)
 (libraries core_unix menhirLib yojson)
 (preprocess
  (pps ppx_inline_test)))

